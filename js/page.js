 /* const languageSelect = document.getElementById('languageSelect');
  const userLang = localStorage.getItem('lang') || 'en';

  // Sett valgt språk basert på tidligere valg
  languageSelect.value = userLang;

  languageSelect.addEventListener('change', function () {
    const selectedLang = this.value;
    localStorage.setItem('lang', selectedLang);

    // Her kan du enten oversette dynamisk eller laste en annen side
    // Eksempel: last inn ny side basert på valgt språk
    // window.location.href = `?lang=${selectedLang}`;

    alert(`Language switched to ${selectedLang.toUpperCase()}`);
  }); */